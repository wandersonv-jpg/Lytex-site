# Diagnóstico de publicação

O domínio oficial registrado no projeto é `https://lytexportf-a6k9ulku.manus.space` (sem o segundo `ex` em `lytexportf`). Ao ser consultado, ele retorna `Site unavailable due to unpaid billing` e orienta atualizar a cobrança no Manus.

A variação `https://lytexexportf-a6k9ulku.manus.space` (com `ex` adicional) não é o domínio registrado e retorna uma tela de manutenção.

A prévia do projeto continua carregando normalmente e contém o telefone `(19) 99622-0753`, os links para `wa.me/5519996220753` e o QR Code do site. Portanto, a indisponibilidade pública não é causada pelo código da página; é um bloqueio no ambiente de publicação do domínio oficial.
